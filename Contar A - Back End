function contarA(palavra){
  let qtd = 0;
  for(let i = 0; i < palavra.length; i++){
    if(palavra[i] == "a" || palavra[i] == "A"){
      qtd++;
    }
  }
  return qtd;
}
console.log(contarA("atual"))
